# Copy to .env.local (never commit real secrets).
# Google / X OAuth client secrets go in Supabase Dashboard → Authentication → Providers only.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
